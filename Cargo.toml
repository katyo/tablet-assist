[workspace]
resolver = "2"
members = ["service"]

[profile.release]
opt-level = "z"
#debug = "limited"
strip = "symbols"
debug-assertions = false
overflow-checks = false
lto = "fat"
#panic = "abort"
incremental = false
codegen-units = 1
